import { CustomTitleDirective } from './custom-title.directive';

describe('CustomTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTitleDirective();
    expect(directive).toBeTruthy();
  });
});
